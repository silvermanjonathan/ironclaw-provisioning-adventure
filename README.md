# Value Date — a Choose Your Own Provisioning adventure

Static multi-page gamebook. Each page is its own HTML file — edit any page freely; `engine.js` carries the reader's ledger between pages in the URL (`?s=...`). Deploy the whole folder to GitHub Pages.

*One page in this book is reachable by no link at all.*
